# BIOStacks Queries

GET sentences containing classA classB
GET sentences containing classA wordA
GET classA which has same span as syntaxA

# Finding relationC where
# classA is any class in the Domain
# classB is any class in the Range
# relationC is implied by wordC
# Other filters that may matter:
# - Section
# - 
GET classA which is the same span as syntaxA AND classB which is the same span as syntaxB AND wordC which is same span as syntaxC

